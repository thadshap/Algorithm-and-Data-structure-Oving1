public class Result {
    int startDate, endDate, maxSum;
    Result(int startDate, int endDate, int maxSum){
        this.startDate = startDate;
        this.endDate = endDate;
        this.maxSum = maxSum;
    }
}
